# 📌 Vue Router 개념 정리

오늘은 **Vue Router**로 라우팅을 어떻게 다루는지 정리했다.
단순히 `RouterLink` 쓰는 법만이 아니라,

* **Routing이 뭔지**
* **SPA에서 왜 Router가 필요한지**
* **기본 라우팅 → Named Routes → 동적 라우트 → 중첩 라우트**
* **프로그래밍 방식 이동(router.push)**
* **useRoute / useRouter 차이**
* **Navigation Guard(접근 제한, 로그인 체크)**

까지 한 번에 흐름 잡는 걸 목표로 했다.

---

## 1. Routing이란? (왜 필요한가)

### 1) 기본 개념

> **Routing = URL에 따라 어떤 화면(컴포넌트)을 보여줄지 결정하는 과정**

* 사용자가 브라우저 주소창에 `/home`, `/about` 등을 입력하거나
* 링크를 클릭해서 URL이 바뀌면
* 그 URL에 맞는 **페이지(컴포넌트)**를 보여주는 것.

### 2) 옛날 방식 vs SPA 방식

#### ① SSR(Server Side Rendering) 시대

* 예전 전통적인 웹 앱:

  * `/home` → 서버가 HTML 통째로 렌더링해서 돌려줌
  * `/about` → 또 다른 HTML 페이지 통째로 돌려줌
* URL이 바뀌면 **브라우저가 전체 페이지를 다시 로드**한다.
* 장점: SEO, 초기 로딩 빠름
  단점: 페이지 바뀔 때마다 전체 새로 고침이라 UX가 무겁다.

#### ② CSR / SPA 시대

SPA(Single Page Application)에서는

* 실제로는 **index.html 한 장**이 로드된 뒤,
* 나머지는 **자바스크립트가 알아서 화면을 갈아끼우는 방식**.
* URL이 바뀌어도 **브라우저는 새 페이지를 요청하지 않고**,
  프론트 프레임워크(Vue)가 “아, 지금 URL이 `/about`이네? 그럼 About 컴포넌트를 보여줘야지” 하고 화면만 교체.

👉 이때,
“**URL ↔ 컴포넌트 매핑** + **브라우저 히스토리 관리**”를 해주는 애가 바로 **Vue Router**.

---

## 2. Vue Router 기본 세팅 흐름

### 1) 설치 및 프로젝트 구조

Vite로 만든 기본 Vue 프로젝트 기준:

1. 패키지 설치(이미 템플릿에 포함되어 있을 수도 있음)

   ```bash
   npm install vue-router
   ```
2. `src/router/index.js`(또는 `index.ts`) 생성
3. `src/views/` 폴더에 페이지용 컴포넌트 생성

   * `HomeView.vue`
   * `AboutView.vue` 등

### 2) Router 인스턴스 만들기

```js
// src/router/index.js
import { createRouter, createWebHistory } from 'vue-router'
import HomeView from '@/views/HomeView.vue'
import AboutView from '@/views/AboutView.vue'

const router = createRouter({
  history: createWebHistory(import.meta.env.BASE_URL),
  routes: [
    {
      path: '/',
      name: 'home',
      component: HomeView,
    },
    {
      path: '/about',
      name: 'about',
      component: AboutView,
    },
  ],
})

export default router
```

* `path`: URL 경로
* `name`: 라우트 이름(나중에 프로그래밍 이동할 때 편함)
* `component`: 보여 줄 컴포넌트

### 3) main.js에 router 등록

```js
import { createApp } from 'vue'
import App from './App.vue'
import router from './router'

const app = createApp(App)
app.use(router)
app.mount('#app')
```

이제 이 앱은 **“라우터를 사용하는 SPA”**가 된다.

---

## 3. RouterLink & RouterView (기본 라우팅)

### 1) `<RouterView />`

* “**현재 URL에 맞는 컴포넌트를 여기에 보여줘**”라는 자리.
* 보통 `App.vue` 템플릿 안에 한 번만 둔다.

```vue
<template>
  <div id="app">
    <Navbar />
    <RouterView />
  </div>
</template>
```

### 2) `<RouterLink />`

* `<a href="/about">` 대신 쓰는 **뷰용 링크 컴포넌트**.
* 특징:

  * 새로고침 없이 SPA 내부에서만 페이지 전환
  * 브라우저 히스토리에 기록 남음(뒤로/앞으로 가능)

```vue
<template>
  <nav>
    <RouterLink to="/">Home</RouterLink>
    <RouterLink to="/about">About</RouterLink>
  </nav>
</template>
```

* `to`에 **path 문자열** 또는 **route 객체**를 넣을 수 있다. (route 객체는 밑에서)

---

## 4. “라우팅 설정 → 화면까지” 기본 흐름 한 번에

1. `router/index.js`에서 `routes` 배열에 path, name, component 등록
2. `App.vue`에 `<RouterView />` 배치
3. 메뉴/버튼에 `<RouterLink to="...">` 작성
4. 사용자가 링크 클릭 → URL 변경
5. Vue Router가 URL에 맞는 컴포넌트를 찾아 `<RouterView />` 위치에 렌더링

---

## 5. Named Routes (이름으로 이동하기)

### 1) path만 쓰는 방식의 한계

* 처음엔 그냥 이렇게 쓰게 된다:

```vue
<RouterLink to="/users/1">User1</RouterLink>
```

* 그런데 나중에 `/users/:id` 구조를 `/member/:id` 로 바꾸고 싶으면,
  이 path를 쓰는 곳을 전부 찾아서 수정해야 한다.

### 2) name으로 라우팅하기

라우트 정의에 `name`을 넣어두면,

```js
const routes = [
  {
    path: '/users/:id',
    name: 'user',
    component: UserView,
  },
]
```

다음처럼 **객체 형태**로 이동할 수 있다.

```vue
<!-- 템플릿에서 -->
<RouterLink :to="{ name: 'user', params: { id: 1 } }">
  User1
</RouterLink>
```

* path 문자열 대신 name + params를 쓰면

  * 나중에 path를 `/profile/:id`로 바꿔도
  * name을 그대로 유지하는 한, 이 코드는 안 고쳐도 된다.

> 정리
>
> * **URL 구조가 바뀔 수 있는 프로젝트에서는 웬만하면 Named Route를 쓰는 게 좋다.**

---

## 6. Dynamic Route Matching (동적 라우팅)

### 1) 문제 상황

예를 들어 `/users/1`, `/users/2`, `/users/3` 같은 프로필 페이지를 만들고 싶을 때:

* 하나하나 라우트를 만들면?

  * `/users/1`, `/users/2`, `/users/3` … 말도 안 됨.

### 2) 해결: URL 일부를 “변수”로 취급

라우터 설정:

```js
{
  path: '/users/:id',
  name: 'user',
  component: UserView,
}
```

* `:id` 부분이 **URL 파라미터(params)**가 된다.

### 3) 사용자가 들어온 URL에서 params 읽기

#### ① 링크로 이동할 때

```vue
<RouterLink :to="{ name: 'user', params: { id: 10 } }">
  User 10
</RouterLink>
```

→ 최종 URL: `/users/10`

#### ② 컴포넌트 안에서 현재 id 사용하기 (Composition API)

```vue
<script setup>
import { useRoute } from 'vue-router'

const route = useRoute()
const userId = route.params.id  // 문자열 형태
</script>

<template>
  <div>
    User ID: {{ userId }}
  </div>
</template>
```

이렇게 동적 라우트는

* 라우트 설정에서 `:id` 처럼 변수 선언
* `useRoute().params.id` 로 읽어오기

이 두 가지가 세트라고 이해하면 된다.

---

## 7. Nested Routes (중첩 라우트)

예를 들어, 유저 페이지 안에 다시 탭처럼

* `/users/:id/profile`
* `/users/:id/posts`

를 넣고 싶을 수 있다.

### 1) 상위/하위 라우트 구조

```js
{
  path: '/users/:id',
  component: UserView,
  children: [
    {
      path: 'profile',
      name: 'user-profile',
      component: UserProfile,
    },
    {
      path: 'posts',
      name: 'user-posts',
      component: UserPosts,
    },
  ],
}
```

* `children` 배열이 **중첩 라우트**.
* children의 `path`는 앞에 `/`를 붙이지 않고 상대 경로로 적는다.

  * 실제 URL: `/users/:id/profile`, `/users/:id/posts`

### 2) UserView 안에서 또 RouterView 사용

```vue
<!-- UserView.vue -->
<template>
  <div>
    <h2>User {{ $route.params.id }}</h2>

    <RouterLink :to="{ name: 'user-profile', params: { id: $route.params.id } }">
      Profile
    </RouterLink>
    <RouterLink :to="{ name: 'user-posts', params: { id: $route.params.id } }">
      Posts
    </RouterLink>

    <!-- 여기 아래에 자식 라우트가 렌더링됨 -->
    <RouterView />
  </div>
</template>
```

* 상위 컴포넌트(UserView)가 “레이아웃” 역할
* 그 안의 `<RouterView />`에 Profile / Posts가 갈아 끼워진다.

> 포인트
>
> * 중첩 라우트 = “한 페이지 안에 또 탭이나 서브 페이지가 있는 구조”를
>   깔끔하게 표현할 수 있는 방법.

---

## 8. Programmatic Navigation (코드로 이동시키기)

지금까지는 `<RouterLink>`로만 이동했다.
근데 가끔은 **버튼 클릭 후 폼 전송 → 성공하면 목록 페이지로 이동**
같이 “자바스크립트 로직 안에서 페이지 이동”이 필요하다.

### 1) useRouter()

```js
import { useRouter } from 'vue-router'

const router = useRouter()
```

* `useRoute()`가 “현재 URL 정보 읽기”용이라면,
* `useRouter()`는 “URL을 바꾸는 조작” 용이라고 생각하면 된다.

### 2) router.push()

```js
router.push('/home')
// 혹은
router.push({ name: 'home' })
// 혹은
router.push({ name: 'user', params: { id: 3 } })
```

* 브라우저 히스토리 스택에 **새 기록 하나 추가** (뒤로 가기 가능)
* `<RouterLink to="...">`를 누르는 것과 같은 효과.

### 3) router.replace()

```js
router.replace({ name: 'login' })
```

* **현재 기록을 새 URL로 교체** (뒤로 가기 시 이전 페이지로 못 돌아감)
* 예: 로그인 후 특정 페이지로 리다이렉트하고, 뒤로 가기로 다시 로그인 화면 못 돌아오게 하고 싶을 때.

---

## 9. route vs router 정리 (useRoute vs useRouter)

헷갈리기 쉬워서 표로 정리했다.

| 함수            | 반환값             | 역할                 |
| ------------- | --------------- | ------------------ |
| `useRoute()`  | **route 객체**    | 현재 URL에 대한 “정보 읽기” |
| `useRouter()` | **router 인스턴스** | URL 변경, 이동 제어      |

### 1) route 객체에서 자주 쓰는 것들

```js
const route = useRoute()

route.path        // 현재 경로 문자열
route.params      // 동적 파라미터 (예: { id: '3' })
route.query       // 쿼리 스트링 (?page=2 같은 것)
route.name        // 현재 라우트 name
```

### 2) router 인스턴스에서 자주 쓰는 것들

```js
const router = useRouter()

router.push(...)
router.replace(...)
router.back()    // 뒤로 가기
router.forward() // 앞으로 가기
```

---

## 10. Navigation Guard (네비게이션 가드)

> 특정 URL로 이동하기 **전/후에**
> “이동해도 되는지” 확인하거나,
> 이동 시 공통 로직을 실행하고 싶을 때 사용하는 기능.

대표적으로:

* **로그인 안 한 사용자는 /mypage 못 들어가게 막기**
* 페이지 전환 시 마다 **로딩 스피너**, **로그 기록**, **스크롤 위치 초기화** 등 처리

### 1) Global Guard (전역 가드)

프로젝트 전체 라우팅에 공통으로 적용되는 가드.

#### beforeEach

```js
// router/index.js
router.beforeEach((to, from) => {
  // to: 이동하려는 라우트
  // from: 현재 라우트
  // return:
  //   - false: 이동 취소
  //   - 다른 경로: 그 경로로 redirect
  //   - 아무것도 안 하면 정상 진행
})
```

#### 예시: 로그인 필요한 페이지 막기

1. 라우트에 메타 정보 달기

```js
{
  path: '/profile',
  name: 'profile',
  component: ProfileView,
  meta: { requiresAuth: true },
}
```

2. beforeEach에서 체크

```js
import { useUserStore } from '@/stores/user'

router.beforeEach((to, from) => {
  const userStore = useUserStore()

  if (to.meta.requiresAuth && !userStore.isLoggedIn) {
    // 로그인 안 되어 있으면 로그인 페이지로 리다이렉트
    return { name: 'login' }
  }
})
```

이렇게 하면 **로그인 안 된 상태에서는 /profile로 직접 URL 쳐도 접근 불가**.

### 2) Per-route Guard (개별 라우트 전용 가드)

특정 라우트에만 `beforeEnter`를 지정할 수 있다.

```js
{
  path: '/admin',
  name: 'admin',
  component: AdminView,
  beforeEnter: (to, from) => {
    const userStore = useUserStore()
    if (!userStore.isAdmin) {
      return false // 이동 취소
    }
  },
}
```

* 전체 앱이 아니라 **특정 라우트**에만 가드를 적용하고 싶을 때 사용.

### 3) In-component Guard (컴포넌트 가드)

* Options API 기준으로는 `beforeRouteEnter`, `beforeRouteLeave` 등 컴포넌트 내부에 정의.
* “이 컴포넌트 떠나기 전에 진짜 나갈래?” 같은 확인창 구현에 사용.

예: (Options API 스타일 예시 느낌)

```js
export default {
  data() {
    return {
      hasUnsavedChanges: false,
    }
  },
  beforeRouteLeave(to, from, next) {
    if (this.hasUnsavedChanges && !window.confirm('저장 안 했는데 나갈까요?')) {
      next(false)
    } else {
      next()
    }
  },
}
```

Composition API에서는

* `onBeforeRouteLeave` 같은 전용 훅 또는
* 그냥 `watch(route)` / `router.beforeEach`를 섞어서 구현할 수 있다.

---

## 11. 오늘 정리한 핵심 포인트 한 줄 요약

* **Routing**: URL ↔ 컴포넌트를 연결해 주는 과정 (SPA에 필수)
* **Vue Router 기본**: `createRouter`, `routes`, `<RouterView>`, `<RouterLink>`
* **Named Route**: path 대신 name + params로 이동 → URL 구조 바뀌어도 안전
* **Dynamic Route**: `:id` 같은 params + `useRoute().params`
* **Nested Route**: 상위 레이아웃 + 내부 탭 구조를 깔끔하게 표현
* **Programmatic Navigation**: `useRouter().push/replace`로 JS 코드에서 이동
* **route vs router**: 읽기용(useRoute) vs 조작용(useRouter)
* **Navigation Guard**: 전역/개별/컴포넌트 가드로 로그인 체크, 권한 체크, 이동 제어

---
