
---

# 📌 Vue Basic Syntax 01 — 템플릿 문법 완전 정리

오늘 정리한 건 한 줄로 말하면:

> “Vue 템플릿 안에서 **데이터를 표시하고**,
> **속성/이벤트/폼**에 **반응형 데이터**를 연결하는 기본 문법들”

핵심 키워드
`{{ }}` · `v-bind` · `v-on` · 디렉티브(directive) · 클래스/스타일 바인딩 · 이벤트 수정자 · `v-model`

---

## 1. 템플릿 문법 전체 그림

Vue는 “DOM을 직접 건드리는 대신 **템플릿**으로 화면을 선언”하고,
템플릿 안에서 **반응형 데이터(ref, reactive)** 를 엮어준다.

템플릿에서 하는 일은 크게 4가지:

1. **텍스트 표시** → `{{ message }}`
2. **HTML 속성에 값 바인딩** → `v-bind:src="imageUrl"` 혹은 `:src="imageUrl"`
3. **이벤트 연결** → `v-on:click="onClick"` 혹은 `@click="onClick"`
4. **폼 입력과 데이터 연결** → `v-model="inputText"`

이 네 가지가 익숙해지면 Vue 템플릿은 거의 다 쓸 수 있다.

---

## 2. 텍스트 출력: Interpolation & Raw HTML

### 2-1. Interpolation `{{ }}`

```vue
<template>
  <p>{{ message }}</p>
</template>

<script setup>
const message = ref('Hello Vue!')
</script>
```

* `{{ }}` 안에서는 **JavaScript 표현식** 사용 가능

  * `{{ number + 1 }}`
  * `{{ ok ? 'YES' : 'NO' }}`
  * `{{ message.split('').reverse().join('') }}` 등
* 하지만 **문(statement)** 은 안 됨

  * `if`, `for`, `const` 선언 같은 건 금지

> 템플릿 안에서는 “값을 계산해서 반환하는 한 줄짜리 표현식”만 쓸 수 있다고 기억하기.

---

### 2-2. Raw HTML – `v-html`

기본적으로 `{{ '<strong>hi</strong>' }}` 이렇게 쓰면
브라우저에는 **태그가 문자 그대로** 보여진다.

실제 HTML로 렌더링하고 싶을 때:

```vue
<template>
  <p v-html="rawHtml"></p>
</template>

<script setup>
const rawHtml = ref('<span style="color:red">This is red</span>')
</script>
```

* 정말 HTML을 “그대로” 렌더링한다.
* **XSS 공격** 위험이 있기 때문에,

  * 사용자 입력을 그대로 넣는 건 위험하고,
  * 신뢰 가능한 HTML일 때만 사용해야 한다.

---

## 3. 속성 바인딩: `v-bind` & JavaScript 표현식

### 3-1. 기본 속성 바인딩

```vue
<img v-bind:src="imageUrl" />
<!-- shorthand -->
<img :src="imageUrl" />
```

* HTML 속성 값에 **반응형 데이터**를 연결한다.
* 문자열뿐 아니라 **숫자, boolean, 객체, 배열**도 바인딩 가능.

예)

```vue
<a :href="linkUrl" :title="`Go to ${linkText}`">{{ linkText }}</a>
```

### 3-2. 동적 attribute 이름

속성 이름 자체를 동적으로 만들 수도 있다.

```vue
<button :[attrName]="value">버튼</button>

<script setup>
const attrName = ref('data-id')
const value = ref(10)
</script>
```

→ 최종 결과: `<button data-id="10">버튼</button>`

* `:[ ]` 안은 **JavaScript 표현식**이므로, 변수/연산 가능.

---

### 3-3. JavaScript 표현식 규칙

템플릿에서 쓸 수 있는 표현식 예시:

```vue
<p>{{ number + 1 }}</p>
<p>{{ ok ? 'YES' : 'NO' }}</p>
<p :id="`item-${id}`"></p>
```

주의할 점:

* “한 줄짜리 표현식”만 가능 → `if (...) { ... }` 같은 문은 안 됨
* 템플릿은 최대한 **간단한 표현**만 쓰고,
  복잡한 건 **computed / methods**로 빼는 게 좋다.

---

## 4. Directive(디렉티브) 개념

Vue 템플릿에서 `v-` 로 시작하는 특수 속성들을 **디렉티브(directive)** 라고 부른다.

예시:

```vue
<p v-if="ok">보여줄지 말지</p>
<a v-bind:href="url">링크</a>
<button v-on:click="onClick">클릭</button>
<input v-model="text" />
```

역할:

> “DOM에 무언가 **행동/기능**을 부여하는 속성”

---

### 4-1. 디렉티브 구조 뜯어보기

예: `v-on:submit.prevent="onSubmit"`

* **Name** : `v-on`
* **Argument** : `submit`
* **Modifier** : `.prevent`
* **Value** : `"onSubmit"` (실제로는 JS 표현식)

이 패턴으로 생각하면 편하다:

```text
v-이름:인자.수정자="값"
```

각 요소의 의미:

* **Name (이름)** : `v-if`, `v-for`, `v-bind`, `v-on` 같은 디렉티브의 종류
* **Argument (인자)** : `v-on:click` 의 `click`, `v-bind:href` 의 `href`
* **Modifier (수정자)** : `.prevent`, `.stop`, `.once`, `.enter` 같이 동작을 바꾸는 옵션
* **Value (값)** : JavaScript 표현식

  * 대부분 `변수명`, `함수호출`, `삼항연산` 등

---

## 5. `v-bind` 고급 – Class & Style 바인딩

클래스/스타일을 **데이터로 제어**하는 패턴이 굉장히 많이 쓰인다.

---

### 5-1. Class를 객체(Object)로 바인딩

```vue
<div :class="{ active: isActive, 'text-danger': hasError }">
  Text
</div>

<script setup>
const isActive = ref(true)
const hasError = ref(false)
</script>
```

결과:

* `isActive = true`, `hasError = false` → `class="active"`
* `isActive = true`, `hasError = true` → `class="active text-danger"`

규칙:

* 객체의 **key = class 이름**
* **value = true/false**

  * true인 것만 실제 class로 들어간다.

static class와 함께 섞어 쓰기:

```vue
<div class="static" :class="{ active: isActive }">Text</div>
```

---

### 5-2. Class를 배열(Array)로 바인딩

```vue
<div :class="[activeClass, errorClass]">Text</div>

<script setup>
const activeClass = ref('active')
const errorClass = ref('text-danger')
</script>
```

또는 조건부로:

```vue
<div :class="['static', isActive ? 'active' : '', errorClass]">
  Text
</div>
```

* 배열 안에 문자열 또는 객체를 섞어서도 사용 가능.

---

### 5-3. Style을 객체(Object)로 바인딩

```vue
<div :style="{ color: activeColor, fontSize: fontSize + 'px' }">
  Text
</div>

<script setup>
const activeColor = ref('crimson')
const fontSize = ref(20)
</script>
```

* CSS 속성 이름은 **camelCase** 또는 `'font-size'` 처럼 문자열 사용 가능.

---

### 5-4. Style을 배열(Array)로 바인딩

```vue
<div :style="[baseStyle, overrideStyle]">Text</div>

<script setup>
const baseStyle = ref({
  color: 'crimson',
  border: '1px solid black',
})
const overrideStyle = ref({
  fontSize: '20px',
})
</script>
```

* 여러 스타일 객체를 합쳐서 적용하고 싶을 때 유용하다.

---

## 6. 이벤트 처리: `v-on` / `@`

### 6-1. 기본 사용

```vue
<button v-on:click="count++">+</button>
<!-- shorthand -->
<button @click="count++">+</button>
```

* DOM 이벤트에 **리스너를 연결**한다.
* `@이벤트명="표현식 또는 메서드명"` 형태

---

### 6-2. Inline Handler vs Method Handler

#### 1) Inline Handler (템플릿 안 한 줄)

```vue
<button @click="count++">+</button>
```

* 간단한 경우에만 사용
* 템플릿이 지저분해지지 않는 선에서만 활용

#### 2) Method Handler (함수로 분리)

```vue
<script setup>
const count = ref(0)

const increase = () => {
  count.value++
}
</script>

<template>
  <button @click="increase">+</button>
</template>
```

* 로직이 길어질수록 method로 분리하는 게 좋다.
* 테스트, 재사용, 가독성 면에서 훨씬 유리.

---

### 6-3. 인자/이벤트 전달

#### 1) 기본 동작: `event` 자동 전달

```vue
<script setup>
const onClick = (event) => {
  console.log(event.target) // 클릭된 요소
}
</script>

<template>
  <button @click="onClick">클릭</button>
</template>
```

#### 2) 직접 인자 넘기기

```vue
<script setup>
const greet = (name) => {
  console.log(`Hello, ${name}`)
}
</script>

<template>
  <button @click="greet('Vue')">인사</button>
</template>
```

#### 3) 인자 + event 둘 다 필요할 때 – `$event`

```vue
<script setup>
const warn = (message, event) => {
  if (event) event.preventDefault()
  alert(message)
}
</script>

<template>
  <button @click="warn('조심!', $event)">경고</button>
</template>
```

* `$event` 가 실제 DOM 이벤트 객체를 가리킨다.

---

## 7. 이벤트 수정자(Event Modifiers & Key Modifiers)

### 7-1. Event Modifiers

`@click.stop`, `@submit.prevent` 등으로
`event.preventDefault()` 나 `event.stopPropagation()` 같은 동작을
템플릿에서 선언적으로 쓸 수 있다.

대표 예:

* `.prevent` : 기본 동작 막기
* `.stop` : 이벤트 버블링 중단

```vue
<form @submit.prevent="onSubmit">
  ...
</form>
```

```vue
<a href="https://google.com" @click.prevent>
  구글로 이동 안 함
</a>
```

chaining도 가능:

```vue
<button @click.stop.prevent="doSomething">클릭</button>
```

→ `stopPropagation` + `preventDefault` 둘 다 적용.

---

### 7-2. Key Modifiers (키보드 이벤트용)

키보드 이벤트에서 특정 키에만 반응하게 하고 싶을 때:

```vue
<input @keyup.enter="onEnter" />
```

* 사용자가 Enter를 눌렀을 때만 `onEnter` 실행.

여러 키 조합:

```vue
<textarea @keydown.ctrl.enter="submit"></textarea>
```

* Ctrl + Enter 를 동시에 눌렀을 때만 `submit` 실행.

---

## 8. Form Input 바인딩: `v-bind` + `v-on` → `v-model`

### 8-1. 수동 양방향 바인딩

직접 하면 이렇게 해야 한다:

```vue
<script setup>
const inputText = ref('')
</script>

<template>
  <input
    :value="inputText"
    @input="inputText = $event.target.value"
  />
</template>
```

* `:value` 로 초기값 바인딩
* `@input`에서 사용자가 입력할 때마다 값을 다시 state에 대입

---

### 8-2. `v-model` = 위 둘의 축약형

```vue
<script setup>
const inputText = ref('')
</script>

<template>
  <input v-model="inputText" />
  <p>입력값: {{ inputText }}</p>
</template>
```

`v-model="inputText"` 는 내부적으로

```vue
:value="inputText" + @input="inputText = $event.target.value"
```

를 자동으로 연결해 주는 디렉티브라고 생각하면 된다.

---

### 8-3. 다양한 input 타입에서의 v-model

1. **Text / textarea**

```vue
<input v-model="text" />
<textarea v-model="content"></textarea>
```

2. **Checkbox (단일 체크박스 – boolean)**

```vue
<input type="checkbox" v-model="checked" />
<p>{{ checked }}</p>

<script setup>
const checked = ref(false)
</script>
```

3. **Checkbox 그룹 (배열)**

```vue
<script setup>
const checkedNames = ref([])
</script>

<template>
  <label>
    <input type="checkbox" value="Alice" v-model="checkedNames" /> Alice
  </label>
  <label>
    <input type="checkbox" value="Bella" v-model="checkedNames" /> Bella
  </label>

  <p>Checked names: {{ checkedNames }}</p>
</template>
```

* 체크된 값들이 **배열**에 쌓인다.

4. **Radio**

```vue
<script setup>
const picked = ref('A')
</script>

<template>
  <label><input type="radio" value="A" v-model="picked" /> A</label>
  <label><input type="radio" value="B" v-model="picked" /> B</label>
  <p>Picked: {{ picked }}</p>
</template>
```

5. **Select**

```vue
<script setup>
const selected = ref('')
</script>

<template>
  <select v-model="selected">
    <option disabled value="">선택하세요</option>
    <option>A</option>
    <option>B</option>
  </select>

  <p>Selected: {{ selected }}</p>
</template>
```

* 다중 선택(multiple)도 가능하며, 그 경우 배열이 된다.

> 작은 팁:
> input에 `v-model` 쓸 때, 한글 IME(조합 중인 글자) 때문에
> 값이 언제 확정되는지 살짝 헷갈릴 수 있는데,
> 자세히 따지는 건 나중에, 지금은 “사용자 입력값이 실시간으로 상태와 연결된다” 정도로 이해해도 충분하다.

---

## 9. 오늘 TIL 요약 정리

### 1) 텍스트/HTML 출력

* `{{ message }}` : 기본 텍스트 출력 (HTML 이스케이프됨)
* `v-html="rawHtml"` : 문자열을 실제 HTML로 렌더링 (보안 주의)

### 2) 속성 & 표현식

* `v-bind:속성="표현식"` / `:속성="표현식"`
* 동적 attribute 이름: `:[attrName]="value"`
* 템플릿 안에서는 **한 줄짜리 JS 표현식만** 가능

### 3) Directive 구조

* `v-이름:인자.수정자="값"`
* 예: `v-on:submit.prevent="onSubmit"`

### 4) Class / Style 바인딩

* 객체: `:class="{ active: isActive }"`, `:style="{ color: activeColor }"`
* 배열: `:class="[activeClass, errorClass]"`, `:style="[baseStyle, overrideStyle]"`

### 5) 이벤트 처리

* `v-on:click="handler"` / `@click="handler"`
* Inline vs Method handler
* 인자 전달: `@click="greet('hi')"`
* 이벤트 객체: `@click="handler($event)"`

### 6) 이벤트/키 수정자

* `.prevent`, `.stop`, …
  `@submit.prevent="onSubmit"`, `@click.stop="onClick"`
* 키 수정자: `.enter`, `.ctrl.enter`
  `@keyup.enter="onEnter"`

### 7) 폼 & v-model

* `v-bind:value` + `v-on:input`의 축약형 = `v-model`
* text/textarea/checkbox/radio/select 모두 지원
* checkbox 단일 → boolean
  checkbox 그룹, select multiple → 배열

---
